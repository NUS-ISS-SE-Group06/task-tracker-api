server:
  port: 8688
  servlet:
    contextPath: /api

spring:
  datasource: #jdbc:sqlite://Users/onggowahyudi/nus/task-tracker-api/database.db
    url: jdbc:sqlite:memory:myDb?cache=shared
    driver-class-name: org.sqlite.JDBC
    username:
    password:
  jpa:
    database-platform: org.hibernate.community.dialect.SQLiteDialect
    hibernate:
      ddl-auto: update
    show-sql: true
  devtools:
    restart:
      enabled: true


