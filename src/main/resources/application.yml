server:
  port: 8688
  servlet:
    contextPath: /api

spring:
  datasource:
    url: jdbc:sqlite:/Users/balajisivaprakasam/Desktop/SiBa/NUS_Projects/task-tracker-api/resources/db/tasktracker.db
    driver-class-name: org.sqlite.JDBC
    username:
    password:
  jpa:
    database-platform: org.hibernate.community.dialect.SQLiteDialect
    hibernate:
      ddl-auto: update
    show-sql: true
  devtools:
    restart:
      enabled: true


